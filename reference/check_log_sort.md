# Check if the supplied `sort` is a valid logging sorting option.

Check if the supplied `sort` is a valid logging sorting option.

## Usage

``` r
check_log_sort(sort)
```

## Arguments

- sort:

  how to sort the entries in a log. Valid are `smart`, `none` and
  `alphabetic`

## Value

No return value, called for side effects
